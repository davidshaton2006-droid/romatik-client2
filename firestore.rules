rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true; // Insecure rules — auth is custom (staff login), not Firebase Auth
    }
  }
}
